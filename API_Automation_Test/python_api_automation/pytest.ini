[pytest]
addopts =
    -v
    --strict-markers
    --disable-warnings
    --html=reports/report.html
    --self-contained-html

testpaths =
    tests

python_files =
    test_*.py

markers =
    smoke: quick smoke tests
    regression: regression test suite
    security: security and misuse scenarios
    contract: API contract and schema validations
